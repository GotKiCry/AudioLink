# Product

<!-- impeccable:product-schema 1 -->

## Platform

adaptive

> 一个产品的两个原生前端：**Windows 桌面（Tauri 2 + React 19，WebView 渲染）是主战场**，
> **Android（Kotlin + Compose）是听筒端**。两端共享同一套产品真相与状态语义，
> 但各自遵循所在平台的交互惯例（桌面：鼠标/键盘/窗口；Android：触控/手势/系统返回）。

## Users

**主用户**：一台常开 Windows PC 作为音频源的技术使用者。他坐在电脑前完成配置与推流，
之后整晚不看界面；判断「有没有正常工作」靠余光一瞥，不靠读数字。
**次用户**：同一人换到沙发/床上，用 Android 手机当听筒 —— 此时他只做三件事：
开始接收、停止接收、调音量。多余信息在这一刻全是噪声。

## Product Purpose

把任意节点的声音，**低延迟、可同步、可混合**地推给局域网内任意数量的其他节点。
成功 = 端到端 P50 80–110 ms（Wi-Fi）/ 40–55 ms（有线）、组内偏差 ≤ ±10 ms（P95）、
8 h 长时稳定无静音、**并且使用者在整段播放里一次都不需要打开界面**。

## Positioning

与上游 AudioShare / AudioPipe 的差别不在外观而在机制，界面必须让这些机制**可被信任地看见**：
任意节点互推的双向网状拓扑（不只 Windows→Android）、QUIC 传输（数据报音频 + 可靠流控制）、
四时间戳时钟同步 + 预约播放（不是事后 seek 拉平）、抖动缓冲 + 丢包隐藏 + NACK + 码率自适应、
自签证书 TOFU + 6 位 PIN 配对白名单（没有 HTTP 管理面）。

## Operating Context

家用/小型办公 Wi-Fi，可能遇到 AP 客户端隔离（自动发现失效 → 必须能手动输 IP 兜底）；
Windows 10 1809+ / 11，托盘常驻、开机自启、关窗不退；Android 8.0+（minSdk 26），
前台服务 + 常驻通知；一台发送端要喂 ≥8 个接收端，一个接收端要能混 ≥4 路；
中英双语（界面语言跟随系统，可手动切）；低延迟意味着 **UI 永远不能挡住音频链路**。

## Capabilities and Constraints

已确认能力：双向网状互推、多设备并发、临时同步组（组内对齐）、声道路由（立体声/仅左/仅右/左右分派）、
手机系统内录（Android 10+）、麦克风采集、每设备音量与静音、遥测与日志导出、自动更新。
**明确不做**：云音乐/音乐库、灯效、USB/adb 通道、远程 Web 管理页、跨公网穿透、iOS/Web 端。
硬性技术约束：`targetSdk` 必须停在 36（SDK 37 会强制 `ACCESS_LOCAL_NETWORK` 运行时权限，
未授权时局域网收发全失败）；内核为 Rust，两端共用；桌面端 WebView2 基线 chrome120。
未决事实：设备自动发现（mDNS/广播）的 UI 形态尚未落地（M2+）。

## Brand Commitments

名称 **AudioLink**，Apache-2.0，NOTICE 保留上游归属（合规红线，不可移除）。
无既有品牌资产需继承：除 launcher 图标外没有 logo、字体、配色或视觉规范文件。
用户已明确授权**替换整个视觉世界**（含主色与中性色），仅要求跨端一致与产品真相不变。

## Evidence on Hand

`docs/00-overview.md`（定位与边界，用户已确认）、`docs/01-requirements.md`（FR-01…FR-40 与验收标准）、
`docs/08-ui-spec.md`（现行 UI 规格与视觉验收清单 —— 作为**反参考**，其信息架构原则仍有效）、
`docs/manual/`（中英文用户手册与故障排查，文案语料的真实来源）、
`target/evidence/`（真机截图与实测日志）、CI 五个 job 与内核 437 项测试。
不存在的证据：没有真实用户访谈、没有可用性测试记录、没有品牌视觉资产 —— 一切宣称都不得虚构。

## Product Principles

1. **状态优先**：任何时刻一眼看出「在推什么、给谁、质量如何」。首屏回答的是这三问，不是设备信息表。
2. **一键可达**：连接/断开/静音/音量主路径不藏二级菜单；手机端只要三件事就够。
3. **错误必说人话**：不允许出现「未知错误」；每个失败都指名问题与恢复路径，代码只在诊断区出现。
4. **一瞥即懂，不动手**：长时间播放中界面是背景板；状态变化要能被余光捕捉（颜色/呼吸/动效），
   但不制造需要持续注视的噪声。
5. **诊断归诊断**：内核真值（水位、欠载、PLC、缓冲帧）必须如实可得，但默认折叠 ——
   它们服务工程师，不参与首屏叙事。

## Accessibility & Inclusion

沿用 `docs/08-ui-spec.md` §3.5 与 §5 的既定要求并作为硬门槛：
所有交互控件 ≥48 dp（Android）/ 可键盘操作的可见焦点（桌面）；状态不靠颜色单独传达（必须带图标或文字）；
TalkBack 与屏幕阅读器能朗读节点状态与延迟；1.3× 大字体下状态卡片不裁切；
深浅模式下正文对比度 ≥4.5:1、大字号 ≥3:1；桌面端 1024×640 至 3840×2160 不破版，Android 360–840 dp 不破版。
