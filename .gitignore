# ---------- Rust ----------
/target/
**/target/
# Android 交叉编译独立产物目录（避免与桌面 target 互相触发重编）
/target-android/
core/target/
desktop/src-tauri/target/
*.rs.bk
Cargo.lock.orig

# ---------- Node / Tauri ----------
node_modules/
dist/
dist-ssr/
.vite/
desktop/dist/
desktop/node_modules/
pnpm-lock.yaml.bak
# Tauri 每次构建自动生成的权限 schema（不入库）
desktop/src-tauri/gen/schemas/

# ---------- Android ----------
android/.gradle/
android/.gradle-home/          # 本项目隔离的 GRADLE_USER_HOME（避免混入全局私服凭据）
.gradle-home/
android/build/
android/app/build/
android/local.properties
# 发布签名口令（PUBLIC 仓库绝对不入库）；注意 .gitignore 不支持行尾注释
android/keystore.properties
android/.idea/
android/captures/
android/*.apk
android/*.aab
*.jks
*.keystore
android/app/release/
android/.cxx/
android/app/src/main/jniLibs/
!android/app/src/main/jniLibs/.gitkeep

# ---------- 密钥与身份（绝不入库）----------
*.pem
*.key
identity/
secrets/
.env
.env.*
!.env.example

# ---------- 日志与产物 ----------
logs/
*.log
*.csv
*.zip
!/docs/**/*.md

# ---------- IDE / OS ----------
.vscode/
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
