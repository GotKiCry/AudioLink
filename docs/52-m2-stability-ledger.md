# M2 稳定性与质量 —— 验收台账（历史归档）

> 本文件由看板行的 B 字段归档而来（第 95 轮）。看板行只保留「当前状态 + 证据索引 + 待决口径」，
> **过程历史（第 72–94 轮）完整保存在这里**，以免看板行长到没人会读完（改前 16043 字符，是第二长行的 5 倍）。

    @{ T = '[M2] 稳定性与质量：抖动缓冲 + 双发/PLC/NACK + 自适应码率 + 遥测面板'; M = 'M2'; P = 'P2'; S = 'In Progress'; B = '**（第 94 轮，最新）桌面前端补上测试基建：10 文件 / 76 用例，且逐条经「改坏即红」验证**。第 88 轮复核指出 `desktop/src` 20 个源文件 **0 个测试、连测试框架都没有** —— 本轮补上：**vitest 5 + jsdom + @testing-library/react**，配置**独立于 `vite.config.ts`**（那份带 tailwind 与 Tauri dev 端口，测试用不上）；`src/test/setup.ts` 只做两件让断言确定的事（RTL cleanup + 界面语言复位，因 i18n 是模块级单例）；`package.json` 加 `test`（非 watch）+ `test:watch`；**CI 的 desktop job 加一步**（不新建 job，同 job 已有 node+pnpm 与装好的依赖）。**测试质量**：76 个用例分布在types(12) / i18n(8) / reductions(7) / PeerCard(10) / PairDialog(8) / CaptureSourcePanel(7) / TelemetryPanel(10) / StatusStrip(5) /UpdatePanel(6) / i18n-literal-guard(3)，每条都能回答「钉住什么行为」；亮点包括**曲线数学**（峰值贴顶 / 零点贴底 / 全零不画假线 / 无 NaN）、**`UpdatePanel` 的硬边界**（渲染**不自动检查**、二次确认前**不调用**安装命令）、以及**新补的 `i18n-literal-guard`**（剥掉注释后组件内不许出现汉字 —— 补了 `i18n:check` 的缺口，并**自带剥离器自检与扫描文件数自检**，防 glob 写错导致永久假绿）。**「改坏即红」逐处验证**：故意改坏 5 处产品代码（归一化系数 28→14、截断方向 `slice(len-LIMIT)`→`slice(0,LIMIT)`、删掉 `failed` 禁用、去掉确认态、换成中文字面量）**全部被抓**，跑完逐处恢复。**跳过的模块如实列出**（`ipc.ts` 薄封装、`useAudioLink` hook 本体、`App/main`、`SettingsPanel/AboutPanel`、三个只有 1–2 个三元的组件），理由都是「只能写 mock 断言的虚测试」——**不是盖过去了**。为可测性动了 2 处产品代码（**纯抽取 + 加导出，行为不变**）。**顺带**：跑了 `license-audit.ps1` 核新增前端依赖 → Rust 651 / 前端 154 / Android 114 · **denied 0**。**门禁（Lead 独立复核）**：`pnpm test` = **10 files / 76 tests passed**（2.62s）、`pnpm build` 绿（tsc 严格 + i18n 护栏 + vite）。**注**：「改坏即红」的 5 处由队友提供证据，Lead 复核了测试绿与构建绿，**未逐处复验**。提交 e2644b0。 **（第 93 轮，最新）待补 e 已做：CI 有了 soak 门禁，且刻意只接「对负载不敏感」的指标**。新增独立 job **soak-gate**（弱网档 + 120s，注入用 M2 验收口径，外加 `--netem-seed 1` 让**注入序列可复现**）；判据只留两条不敏感项：**会话不断 + 掩盖帧 ≤ 总帧数 1%**；显式 `--max-underrun-pct 0` 让静音占比**只记录不判**（默认值仍是 1%）；**既有判据一条没动**。**为什么严格档不进 CI**：严格档的 `late_drop` / `underrun` 量的是「**本机调度与回环争用**」而非链路质量 —— 同参数同代码，安静 900s = ok（八类全 0）、有负载 = failed（`underrun` 29 / `late_drop` 28），且争用会触发接收侧 ~30.5s 周期的降档节律⇒ **随机变红，比没有门禁更糟**（团队会开始忽略它）。**「不敏感」有现成实证**：两次 120s 正例都是在**同机还跑着 8h 长跑**的情况下跑出 `ok` / `plc 0`。**成本账**（记进 docs/26 §11）：独立 job 与其它 job 并行，≈70s 固定开销 + build 30–90s + soak 125s≈ **225–285s**，与关键路径 core-heavy（≈267s）**同量级 ⇒ 稳态不抬墙钟**；一次性代价是首轮冷 rust-cache（约 +2~4 分钟）。**克制**：没上 300s（那会把关键路径抬到 ~320s、约 +1 分钟/PR），更长的时长应另开 nightly。**退出码管线收紧**：有违规先透传 1；兜底「判定 ok 但采样 < 100 → 红」**只对 ok 生效**，所以「有违规」永远拿得到 1；报告缺失 → 红；报告 `if: always()` 上传 artifact（失败也留证）。**Lead 独立复核**：`soak-gate` 确在 workflow 内且判据确不含敏感项；**亲手跑正例 = `ok samples=118 violations=0 plc=0`、退出码 0** ✓；负例在我这儿跑出 `exit=3`（20s 内恰好无欠载，于是采样兜底生效判红）而非 1 —— **同样表示红**，说明那条兜底是有效双保险，但**该负例依赖欠载发生、不可稳定复现**（记为遗留）。**未验**：真实 runner 的分步读数（docs/26 §11 留了「首轮跑完补表」的提示与估算依据）；CI 侧仅静态检查（本机无 `act`）。提交 3ff2b38。**至此第 88 轮复核给出的 a（前半）/c/d/e 全部完成，只余 a 后半的 8h 结果**。 **（第 92 轮，最新）待补 c 已做：自适应降级有了可复现实验 + 目标码率可见**。新增 `tests/engine/adaptive_bitrate_loss.rs`（开关式高比例丢包中继：发送→接收每 4 包只放行 1 包、反方向照常转发），**连跑 3 次时间线四步完全一致**（峰值丢包 50~61%）、**不依赖机器负载**。**扫档结论**（60s / 宽容档）：2% 注入 → 接收侧 `loss_pct` **恒为 0**、**0 次降级**（双发 + NACK 把洞补齐，这本身是**正确行为**，也解释了为何 8h 弱网报告 481 个粗采样里 468 个 `loss=0`）；**门槛在 10% 量级**（残差 ≈2%，刚越过 §8 的 1% 门槛）→ 10% 触发 2 次、20% 3 次、30% 6 次、50% 3 次，目标最低降到 §8 下限 96000 bps。**观测口径**：只看 `EngineEvent::CodecAdapted`（**目标码率** = 自适应的动作），接收侧实测链路码率 ≈ 目标×2 且含重传噪声、**不能当动作读**；实测目标序列`320000→256000→204800→163840`，缓解后 `→180224`（**先降后升**）。**一条反直觉的坑（已写进测试文件头）**：**不能用「整体断流」制造降级** —— 接收侧丢包率来自「发现序号洞」，一个包都收不到时**没有洞可算**，自适应看到的仍是 0。**可见性**：soak 报告新增 `codec_adaptations[]` / `summary.codec_adapted_count` / `summary.codec_target_bps_final`，控制台新增摘要与时间线；并把「钉死期望值不跟随自适应」做成显式开关（**观测照记、判据不被动摇**，保住「故意写错目标就该红」的自检）。**顺带更正过时文档**：`docs/23` §3.3 的「回环上降级不可复现」已被推翻，原文加了更正块（附扫档表）。**遗留**：desktop UI 仍只把 `CodecAdapted` 写进日志（这条时间线该接到遥测面板）。**门禁（Lead 独立复核）**：fmt 0 / clippy -D warnings 0 / engine+tools **312 passed**。提交 337d4bc。 **（第 91 轮，最新）待补 d 已做：静音占比成为可判定指标（阈值待产品确认）**。新增 `SoakThresholds::max_underrun_pct_x100`（0 = 不判），构造档位时折算成既有 `max_underruns` 绝对预算 ⇒ **判定路径没换**、严格档 `max_underruns = 0` 语义原样；CLI `--max-underrun-pct N` 仅弱网档（严格档用它**直接 exit 2**）。**实测静音占比**（= 累计 underruns ÷ 计划总拍数，总拍数 = 计划秒数×1000÷frame_ms）：弱网 8h 重跑 **10.68%**（153762/1440000）、弱网 8h 首次 **0.30%**、干净回环 900s **0.00%**。**两次 8h 差 35 倍不是链路退化**（重跑集中爆发峰值 1836 拍/min ≈ 那一分钟 61% 在补静音；首次摊在全场 497 拍/min；两者末 10 分钟都归零）⇒ **静音占比与严格档 late_drop 有同一条前提：验收必须独占机器**。**建议值 1.00%**（依据：与弱网档既有的「掩盖帧 ≤ 总帧数 1%」同量级 —— 欠载（补静音）与 PLC（掩盖帧）本就是「这一拍没有真音频」的两半）、**过渡闸门 12.00%**（现状基线之上留余量，**只承诺不退化、不承诺可听**）；两值均标「**待产品确认**」，**未写进验收表**。**新判据「真的在判」的直接证据**（同参数 60s 三次）：默认 1% 安静 0.20% → **ok**；默认 1% 有负载 **9.33%** → **failed**；`--max-underrun-pct 0.1`（预算 3 拍）0.40% → **failed** —— **同参数只改预算判定就翻红**。**「长断音」用现有遥测判不了**（只有累计计数 + 接收侧 1 Hz 快照），如实记为遗留；给出可证下界「一秒 k 拍静音 / 共 B 拍 ⇒ 最长连续静音 ≥ ⌈k ÷ (B−k+1)⌉ 拍」，报告只印「最坏一秒」与该下界并标注**不参与判定**；真正的连续性需在**引擎播放线程**统计并经**事件**上报（不能进冻结的 `StreamStats`）。**抖动口径已钉死**：`--netem-jitter-ms 15` = **±15 ms（峰峰 30 ms）**，验收表的「30 ms 抖动」指峰峰值、**不是 ±30 ms**；`--netem-delay-ms 15` 是固定单向延迟、与抖动两回事。**门禁（Lead 独立复核）**：fmt 0 / clippy -D warnings 0 / tools **109 passed**（soak 单测 18 → 27）。提交 ac8de31。**注**：8h 弱网长跑期间有队友轻负载，其欠载数含噪声、且是旧格式二进制，判静音请用公式离线复算。 **（第 90 轮，最新）待补 a 的前半达成：当前 HEAD 的 900s 干净回环 + 严格档 verdict=ok**。`samples=898`（跑满）、`violations_total=0`、`violations_by_kind` **八类全 0**（含 `late_drop 0` 真迟到归零、`underrun 0`）、`last_violation_at_secs` 为空（全程从未违规）、exit 0。证据 `target/evidence/soak/strict-clean-900s-head.json`（注：`target/evidence/` 在 gitignore 内 —— 证据留本地、结论记看板，与项目既有做法一致）。**对比意义（这条比数字更重要）**：第 88 轮队友用**完全相同的参数**跑出 failed（`underrun` 累计 14、真迟到 8、`depth_drops` 2），差异不是代码而是**机器负载** —— 这次跑前我主动保持机器安静（全程不做任何重活）。这同时印证了第 84/85 轮的「严格档环境前提」结论，也说明第 86 轮的 `depth_drops` 分离是对的（本轮 `迟到 0 / 主动丢 1`，降档那一拍正确地记进了 `depth_drops`，而不是像以前那样污染 `late_drops`）。**a 的后半（8h 弱网、当前 HEAD）已在后台跑**（`soak-8h-head.json`，用弱网档）。余下 c（自适应降级可复现实验）、d（弱网档把「可听 / 无长断音」改写成可判定指标）、e（把长跑接进 CI）未动。 **（第 89 轮，最新）FR-28 已实现 —— 上一轮复核出的那条硬缺口补上了**。新增 `runtime/sink_watchdog.rs`（2 s 窗口 + 链路新鲜度 + 2→30 s 指数退避 + 6 项单测）与 `tests/engine/sink_watchdog.rs`；`runtime.rs` 把 `write_frame`从 `bool` 改成三态 `FrameWrite{Ok,SinkFailed,Fatal}`，**写失败不再杀死播放线程** —— 队友指出旧行为正是「**一次写失败 → 播放线程 break 退出 → 状态仍是 Streaming 但永不出声**」，也就是 FR-28 要根治的那个形态；到点走新增的 `reopen_playout_sink()`（先 `stop()` 旧的、再向工厂要一个新的）并报`EngineEvent::Error{code:2002 SINK_REBUILD}`（**复用既有变体**，desktop/ffi 零改动）。**判据设计**：「成功输出」定义为sink 账本在**真实音频那一拍**增长 >0，于是「写失败」与「收下却不接走（假成功）」在同一个口径下是同一种事实；补静音 / 升档 Hold / 排播等待的拍**不算** —— 那些正是要修的故障，不能拿来当健康证据。**注入法实测（真实双 Engine + 真实 QUIC）**：写失败 → 坏代被喂 98 帧 / 被接走 0 帧、工厂调用 **2** 次、2002 **正好 1 条**（「播放器 2019 ms 没有接走任何一帧音频但链路正常，已重建」）；假成功（`write` 返回 Ok 却不接数据）同样被捕获；**健康对照** → 6 s 内接走 **297 帧**真实音频、工厂调用 **1** 次、2002 **0 条**（不误伤）。**链路也停时不重建**（那是 FR-27 的活，重建 sink 治不了还白扔一个设备）。**重建后的时序处置**：清队列 + `expected_seq=None`游标重启 + 重攒余量 + `next_write` 节奏归位 —— 理由是被停摆那几秒的音频**已经过期**，按旧游标追播只会把永久延迟钉进链路；这些被主动作废的积压**不记 `late_drops`**（自愈动作 ≠ 网络迟到），与「严格档零容忍只给真迟到」的口径一致；§7 排播按 epoch 换算不受影响。**门禁（Lead 独立复核）**：fmt 0 / clippy -D warnings 零告警 / `--test engine` **42 passed**（既有 38 项无回归）/ `--lib` **159 passed**（含 6 项看门狗单测）/ tools 101 passed / ffi+desktop check 干净。提交 0d3d00f。**遗留**：2002 在桌面端仍按既有「统计类提示」处理（`engine_bridge.rs:1305`，本轮未改）—— 是否要把「已重建播放器」显式呈现给用户，待定。 **（第 88 轮，最新）独立复核推翻了 Lead 的转 Done 意图 —— 本项继续 In Progress，且发现看板清单漏了整条 FR-28**。Lead 原准备转 Done（依据：60s 干净回环严格档 verdict=ok、8h 弱网 ok、六部件都有实现）；队友 `depth-drops` 复核后给出 4 条硬事实与 5 条缺口，结论**不建议转 Done**，Lead 采纳。① **本行清单本身漏了 FR-28**：路线图 M2 交付物 4「看门狗与自愈」= FR-28（无输出 2 s 重建 sink）+ FR-27（重连），而本行把交付物 2 拆成三项凑成「六个部件」，**整条 FR-28 被漏掉**；FR-28 在代码里**没有实现** —— 全仓库只有错误码定义、文档、desktop 两处错误码匹配、`session.rs:460` 一处**测试里构造的错误对象**；engine 里 `factory()` 只有两处调用（采集 / 播放），没有任何「重建 sink」路径，也 grep 不到「≥2 s 无输出」探测 ⇒验收表「注入播放器异常后自动重建」**零证据**。② **「六个部件」4 实 2 虚**：实的是抖动缓冲（`jitter.rs` 14 项单测）/ 双发 / PLC / NACK；**虚的是自适应码率的降级分支**（`adaptive_bitrate.rs` 只测「从 96 kbps 升档」，文件头自认降级在回环不可复现；8h 报告 481 个粗采样里468 个 `loss_pct_x100 = 0`，「丢包 >1% 持续 3 s」的降级条件基本不成立；且 desktop 对 `CodecAdapted` **只记日志**，「可见的时间线」在 UI 上不存在）与**遥测面板**（`desktop/src` 20 个源文件 **0 个测试**，`package.json` 里连测试框架都没有）。③ **弱网档「欠载 / 迟到不判」是缺口、不是设计选择**：验收标准写「2% 随机丢包下可懂、无长断音」「8 h 无崩溃 / **无静音**」，而 `underrun` 的定义就是「这一拍没帧 → **补静音**并计数」；8h 报告累计 underrun **153762**（≈5.3 次/秒，几乎每帧一次）；`nack_retransmit.rs:263-265` 自己写着「播放侧仍会有欠载…这是本轮实测到的**已知权衡**」。把 `max_underruns` 设成 `u32::MAX`等于**把未完成项移出判据**。Lead 的「弱网牺牲质量保连通」作为方向可以成立，但必须落成可判定指标（欠载率上限 / 静音占比上限 / PLC ≤ 1%）。④ **两条硬事实**：（a）**当前 HEAD 的 900s 干净严格档仍然 failed**（新证据 `target/evidence/soak/strict-clean-900s-depthdrops.json`：underrun 累计 14、真迟到 8、depth_drops 2）—— **60s 的 ok 不能外推**；（b）**8h 弱网 ok 不是当前代码产出**（那份报告缺第 83 轮才引入的 `bitrate_judged` / `expected_bitrate_bps_final` / `expected_bitrate_updates` 三个字段 ⇒ 跑在第 83 轮之前）。⑤ **CI 里没有任何 soak 步骤**（`.github/workflows/ci.yml` 只有 nextest）⇒「干净链路零容忍」与「8h 无故障」在 CI 上**零门禁**，这让「严格档需独占机器」从一条遗留变成结构性问题（连自动化入口都没有）。⑥ 口径对不上：验收表与 NFR 写「30 ms 抖动」，而三份报告注入的都是 `delay_ms:15 + jitter_ms:15`；「无爆音」至今**没有任何出声证据**（soak 全程 SyntheticCapture + NullPlayout）。**待补（按性价比）**：a. 当前 HEAD 重跑 8h 弱网 + 900s 干净严格档（变绿或显式降级为「已知不稳」并写进口径）；b. **FR-28 实现 + 故障注入，或把交付物 4 拆开、把 FR-28 明确移出 M2 并记账**；c. 自适应降级给可复现实验 + 让 `CodecAdapted` 可见；d. 弱网档把「可听 / 无长断音」改写成可判定指标；e. 把 a 的门禁接进 CI（≥300s 干净严格档 + 短弱网档）。**本轮收获**：一次独立复核挡下了一个会被稀释的 Done，并挖出一条被清单漏掉的硬缺口。 **（第 86 轮，最新）：干净回环 + 严格档终于 verdict=ok** —— 卡了两轮的「降档主动丢帧被记成迟到」已修。① **语义依据**：`late_drops` 的语义应是「帧到得太晚、来不及播」（网络/调度质量问题）；而降档 `DropOldest(1)` 是控制器**主动的策略选择**（用丢最旧一帧换取更低的排队延迟）。两者成因不同，共用一个计数器会让「严格档零容忍」把一个**设计上每次都会发生**的策略代价判成质量违规。② **实现**：新增 `depth_drops`（按「控制器主动要求丢掉的播放拍数」命名），`DropOldest` 路径改记它；真迟到（`take_due_frame` 里落后于播放游标的帧）仍记 `late_drops` —— 4 个 `record_late_drop` 点逐一核过、计数次数未变。③ **刻意不动 wire（重要）**：`audiolink-types` **一字未改** —— `StreamStats` 是 `0x13 STREAM_STATS` 的 postcard 载荷，proto 的 `payload_decode` 要求**恰好消费全部字节**（尾随数据非法），往末尾追加字段会让**旧版本节点解码报错**；所以 `depth_drops` 是**进程内可见**口径（`Engine::depth_drops(peer)`），FFI / Android / desktop / proto 零改动，跨机与 UI 可见性所需的协议演进已记为缺口。④ **效果**：60s 干净回环 + 严格档 `violations_by_kind` **八类全 0**、`total 0`、**verdict ok**；`depth_drops = 1` 出现在 **t=30s**（首次降档那一拍），迟到全程 0。⑤ `tools/soak-report.ps1` 现在同时打印两个数（新报告「迟到丢弃 0 / 主动丢帧 1」；旧报告用 `-` 占位，向后兼容实测）。⑥ `depth_drops_judged: false` —— **阈值口径未设、待产品定**；且第 84/85 轮那条「严格档需独占机器」的前提**仍然成立**（900s 争用仍会触发 ~30.5s 极限环）。门禁（Lead 独立复核）：fmt 0 / clippy -D warnings 零告警（含 ffi+desktop）/ engine lib 153 / engine 38 / tools 101。提交 e82277b。 **（第 85 轮，最新）：上一段「符合机器争用瞬态」不够准确，这里更正**。队友把 900s 的违规**时间分布**拉出来后：t=243s 起 `late_drop` 与 `underrun` **成对且间隔恒为 ~30.5s**（243/274/304/342/…/870），同期水位在 20000↔40000 µs 摆动。**争用只是触发器，振荡是控制器内在节律** —— 争用把目标顶到 2 帧后，每 30 个稳定窗口就降回 1 帧、降档必记 1 条 `late_drop`，形成**自维持的 ~30s 极限环**（间隔过于整齐，与随机负载抖动不同）。前 4 分钟（t=3..240s）在负载到来前完全干净，与安静的 300s 跑一致。**所以「严格档需独占机器」成立但不够**：仅靠独占机器**消不掉 t=30s 那条** —— 它与负载无关、每次必现，根因是降档主动丢帧被记成 `late_drop`（见第 83 轮）。**本轮已落地**：期望码率从硬编码 320000（弱网口径）改为**三态** `--expected-bps auto(默认)/N/0`：auto 取本次**真实使用**的`codec.bitrate_bps × 2`（接收侧遥测按收到字节记账，含双发冗余份数）并订阅 `CodecAdapted` 逐拍跟随（实测跟随 8 次，期望 320000→640000）；`N` 钉死不跟随（保住 docs/22「故意写错目标就该红」的自检语义）；`0` 不判且跟随不会把判据偷偷打开。严格档其余阈值仍全零、码率判据仍开 —— **判的是「收到的码率有没有跟上发送侧实际想发的量」**，不是「码率必须等于某个魔法数」。效果：`bitrate_out_of_range` **878 → 0，且 900s 全程保持 0**。门禁（Lead 独立复核）：fmt 0 / clippy -D warnings 零告警 / nextest -p audiolink-tools **100 passed**。提交 ebe3270。 **（第 84 轮）**：900s 干净回环严格档暴露了一个此前**没写明的前提 —— 严格档只在机器安静时才有意义**。同一份代码两种环境：**300s 安静机器** → `late_drop 1`、其余七类全 0；**900s 期间机器另有负载** →`late_drop 28 + underrun 11`（`bitrate_out_of_range` 保持 0，说明第 83 轮的修复是稳的）。违规**成对成批**出现（队友在 t≈553s 观察到累计欠载 14 / 迟到 14 同步增长），符合「机器争用瞬态」而非稳态缺陷。**这条前提决定了严格档能不能在 CI 或共享开发机上当门禁用**，所以必须显式化：要么在工具与文档里声明「严格档需独占机器」，要么让严格档对负载鲁棒（例如按 CPU 争用程度标注报告可信度）。已记为口径待决项。③ 300s 安静跑的证据依然成立：唯一固定事件是 t=30s 的抖动降档丢帧（见上一节）。④ 本轮结束时队友 `soak-threshold` 的改动（`soak.rs` + `bin/soak_runner.rs`）**尚未提交**（它仍在跑/分析），待其 complete 后由 Lead 复核门禁并**显式指定文件**提交。 **（第 83 轮）**：干净回环严格档的唯一残留已定位到「抖动降档丢帧」的**记账语义**。① `bitrate_out_of_range` 已从 **878 修到 0**：soak-runner 的期望码率改为**跟随编码器目标**，并在报告里新增 `expected_bitrate_source` / `summary.bitrate_judged` / `expected_bitrate_bps_final` /`expected_bitrate_updates` 四个可见字段（顶格的 `expected_bitrate_bps` 保留为起跑种子，`tools/soak-report.ps1` 在读它）——加这几个字段是为了不让下一个人误以为判据没生效（Lead 本人就差点这么以为）。② 剩下的固定违规是 `late_drop: 1`，**时刻恒为 t=30s**（60s / 300s / 900s 三份证据一致）：接收侧 AdaptiveJitterDepth起步 target=2 帧（40 ms），抖动 p95 ≤ 帧长/4 时需**连续 30 个 1 Hz 窗口**（≈30 s）才降一档；降档那一拍`PlayoutDepthState::action` 返回 `DropOldest(1)`，而该路径被记进 `record_late_drop()`。硬证据与时刻吻合：coarse 采样 `buffer_level_us` 在 t=3s 是 40000 µs（2 帧）、t=60s 是 20000 µs（1 帧），同期 late_drops 0→1。**所以它既不是开流预热问题**（不在第 4~8 秒，加长预热不是治本），**也不是稳态质量事件**（t>30s 之后完全干净），而是「降档主动丢帧」与「真迟到」共用一个计数器。300s 全程其余七类（欠载/掩盖/丢包/NACK/not_streaming/stalled/码率）**全为 0**。③ **要不要把它算作违规是产品语义问题**（降档确实丢了一帧音频，但那是降延迟策略的代价），不是测试能定的；修法若要做，应在 engine 侧把两条路径分开记账（如另立 depth_drops），而不是放宽严格档阈值 —— 本项口径待决，故仍未转 Done。 **（第 81 轮）严格档弱网长跑失败 41 项，但根因是判据标定错配、不是产品缺陷**：`--netem-loss-pct 2 --netem-delay-ms 15 --netem-jitter-ms 15 --netem-bandwidth-kbps 5000` 配**严格档**（SoakThresholds 默认为 max_underruns=0 / max_late_drops=0 / max_loss_pct_x100=0，即绝对零容忍）跑 900s →verdict=failed、violations_total=41（packet_loss 12 / playout_concealment 12 / late_drop 8 / underrun 7 / nack_retransmit 2）。**这是判据自相矛盾**：注入了 2% 丢包却要求 loss_pct=0，而丢包的后果（PLC 掩盖、欠载）也必然 > 0 —— 这些违规是物理必然而非实现缺陷。**（第 82 轮更正一处我自己的判断错误）**：上一句里「弱网档等于不判、中间没有档位」是错的 —— 读源码（soak.rs:71-82）可见`SoakThresholds::weak_network()` 把 `max_plc` 钉在**总帧数的 1%**，文档承诺的「掩盖比例 ≤ 1%」是**真的实现了**；41 项违规的真因是**我用严格档（全零阈值，为干净回环设计）去跑注入了 2% 丢包的链路**，属档位错配的用法错误。真正的缺口更窄：弱网档只判 PLC ≤ 1%，而欠载 / 迟到 / NACK / 丢包率在弱网档下**完全不判**（8h 弱网实测累计欠载 153762、迟到 154274），覆盖面偏窄、阈值该定多少需要产品确认 —— 这一条比「判据缺失」准确得多。**（第 82 轮）干净回环 + 严格档 900s：本行少见的强正面证据**。verdict=failed，但**只有一类违规** ——`bitrate_out_of_range: 878`（样本 898），而**欠载 0 / 掩盖 0 / 丢包 0 / NACK 0 / not_streaming 0 / stalled 0 / late_drop 1**。即产品在**无注入丢包**的链路上，播放侧质量指标**全部为零**。那 878 次码率超范围**不是质量问题、是参数错配**：soak-runner 的 expected_bitrate_bps=320000 是弱网口径，而干净链路上自适应把编码器推到 640800 bps（正好 2 倍），超出 30% 容忍 ⇒ 每个采样桶都记一次。两次长跑合起来的定性是：**严格档配干净回环时唯一要修的是 expected_bitrate 的可配置性**，而不是播放质量；弱网场景该用弱网档（PLC ≤ 1%），41 项违规属档位错配。**本行仍未转 Done** ——「弱网下的欠载 / 迟到要不要设上限、上限多少」需要产品确认，不是测试能定的。**处置**：另起一次「干净回环 + 严格档」长跑（无 netem 注入）来回答严格档真正想问的问题 —— 产品在无注入丢包的链路上能否做到零欠载、零迟到；两次结果合并后才给本行定性。上一轮的宽容档 8h 长跑（verdict=ok、violations_total=0）只证明「弱网 8 h 会话不断」，与本节结论不冲突、也不足以支撑本行转 Done。docs/05-roadmap.md M2；验收含 8 h soak 与弱网（5 Mbps / 2% 丢包 / 30 ms 抖动）。2026-09-16：播放游标、PCM 丢包掩盖、自适应抖动/有界重排和默认冗余双发已完成。副本延迟一帧，接收按序号去重且质量指标只取首个有效副本；最高 60 ms 档欠载可同档补水。PC 双 Engine 与 PHK110 真实 QUIC 码率均稳定 320.8 kbps；手机 90 s 队列 P50/P95/max 60 ms，链路丢包/PLC 0，欠载/迟到 24/22 且第 75–90 s 无新增，AudioTrack underrun 0、FastMixer writeErrors 0。NACK 重传、soak-runner、自适应码率与弱网测试台均已落地（docs/21、docs/22、docs/23、docs/24）。M2 弱网口径已实测通过（60 s：2% 丢包 / 15±15 ms / 5 Mbps → exit 0、掩盖 0.13%、全程 streaming）。2026-09-16 遥测面板（5 分钟曲线 + CSV 日志导出）已完成（提交 87cb059 / CI 35079292910 / docs/25）。下一步只剩：**真正跑一次 8 h soak**（工具就绪）、真机弱网与更高丢包档（触发降级）；单声道降级与帧长 10 ms 切换已在 LinkSeverity 记账、留待专门任务。详见 docs/17-m2-pcm-concealment.md、docs/18-m2-adaptive-jitter.md、docs/19-m2-redundant-send.md。2026-09-16：8 h 弱网长跑（2% 丢包 / 15±15 ms / 5 Mbps，--tolerant 判据）已在后台启动，目标 28800 s，报告与注入自账落到 target/evidence/soak/soak-8h-netem.json（+ .netem.json）。长跑期间本机同时有开发编译与测试活动，结论出来时会把这一点一起记下来（不粉饰）。（28800 s，tolerant 档，采样 28798 次）：掩盖率 0.066%（弱网门槛 1%）、注入自账 2.01%、末次漂移 -4 ppm、RTT 41.8 ms、水位 120 ms。长跑逼出并修掉弱网档三个判定缺陷（码率判据未关、0 = 不判 语义与实现相反、快照总量配额被单类独占），详见 weak-network 条目与 docs/22 §9。本项仍为 In Progress：修完判据后需要重跑一次 8 h 才算完成，且真机（PC→Android）长跑验收与 M1 真机延迟项仍待做。 2026-09-17 M2 的 8 h 弱网长跑已执行2026-09-17（第 69 轮）：澄清一份容易误读的历史报告 —— target/evidence/soak/soak-8h-netem.json 的 verdict=failed（200 条全是 bitrate_out_of_range、dropped_violations 6855）是**修复前**的长跑证据（与 docs/22 §9 的 violations_total 7055 对上），**不是**当前判定；同参数 90 s 对照用当前二进制跑出 verdict=ok / violations=0 / dropped=0（报告见 target/evidence/soak/tolerant-90s-current.json）。docs/22 新增 §10 写清「怎么一眼分清历史报告与当前判定」。**（第 80 轮）8 h rerun 已完成，宽容档判定通过**：verdict=ok、violations_total=0（八类违规计数全为 0 —— bitrate_out_of_range / late_drop / nack_retransmit / not_streaming / packet_loss / playout_concealment / stalled / underrun）， 样本 28798 条、planned_seconds=28800（**真的跑满 8 小时**），参数 --netem-loss-pct 2 --netem-delay-ms 15 --netem-jitter-ms 15 --netem-bandwidth-kbps 5000 --tolerant，报告 target/evidence/soak/soak-8h-netem-rerun.json （日志同名 .log）。**口径必须写清**：这是**宽容档** —— 判据是「会话不断 + 掩盖比例 ≤ 1%」，**不是严格档**； 同一份报告里 late_drops 累计 154274、underruns 累计 153762（弱网下的质量损耗真实存在，只是不被宽容档计为违规）， 所以本行**不据此转 Done** —— 严格档判定与完整验收结论仍待补齐。当初挂在这句上的要求是「出报告后回填 8 h 结论」， 现在报告到位、结论如实回填，但「8 h 不崩」与「弱网质量达标」是两件事，不能混为一谈。' }
