{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M1 最小 UI 的权限集：只给主窗口「订阅后端事件」的能力（core:event 的 default 含 listen/unlisten/emit）。外壳自身的命令不经过 ACL（build.rs 未声明 app ACL manifest，见 tauri-2.11 src/webview/mod.rs 的 has_app_acl_manifest 分支），因此这里不列任何 app 权限 —— 最小权限原则。",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
